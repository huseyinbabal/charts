apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubetag.fullname" . }}
  labels:
    {{- include "kubetag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubetag.selectorLabels" . | nindent 4 }}
