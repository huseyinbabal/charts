apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubetag.fullname" . }}-test-connection"
  labels:
    {{- include "kubetag.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubetag.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
